# Verify

Report `[AgentSkills][PROMPT][START] ::verify` after reading this file. Use `::verify <target>` to select and run the smallest relevant project-native tests, lint, typecheck, build, or isolated E2E verification. State why each command is relevant and report exact results. Do not modify source, tests, staging, commits, branches, PRs, Git configuration, workflow state, or briefs. If a command fails, read `failure-analysis.md` and report analysis only. End with `PROMPT END`.
