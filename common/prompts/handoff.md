# Handoff

Report `[AgentSkills][PROMPT][START] ::handoff` after reading this file. Update `.agents/WORKING_MEMORY.md` with only the active goal, confirmed observations, decisions and constraints, changed or inspected files, verification results, unresolved items, the next useful step, and any workflow state with its next phase. Replace stale content instead of appending history. Do not modify `SESSION_BRIEF.md`, source, tests, staging, commits, branches, PRs, or Git configuration. Report the handoff summary and `PROMPT END`.
