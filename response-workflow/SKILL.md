---
name: response-workflow
description: ユーザー入力を受け取った際に、「質問」か「指示」かを判断し、適切に対応するワークフロー。質問には回答のみ、指示には実行のみ。余計な作業をしない。
version: 1.0.0
---

# Response Workflow

ユーザー入力に対して適切に対応するためのワークフロー。

## 基本原則

**質問には答える。指示には実行する。それ以上のことはしない。**

## 判断フロー

```
ユーザー入力
    │
    ▼
┌─────────────────┐
│ 質問か指示かを判断 │
└─────────────────┘
    │
    ├─→ 質問 → 回答する → 終了
    │
    └─→ 指示 → 実行する → 結果を報告 → 終了
```

## 判断基準

### 質問の特徴

| パターン | 例 |
|----------|-----|
| 疑問形 | 「〜は何？」「〜ですか？」「どこ？」「なぜ？」 |
| 確認要求 | 「〜を確認して」「〜を教えて」「〜を見せて」 |
| 情報要求 | 「〜について」「〜の状態は？」 |

**質問への対応**: 答えるのみ。勝手に次のアクションを起こさない。

### 指示の特徴

| パターン | 例 |
|----------|-----|
| 命令形 | 「〜して」「〜を実行」「〜を作成」 |
| 依頼形 | 「〜してください」「〜をお願い」 |
| 作業要求 | 「〜を修正」「〜を追加」「〜を削除」 |

**指示への対応**: 実行する。完了したら結果を報告する。

## 禁止事項

| やってはいけないこと | 理由 |
|----------------------|------|
| 質問に答えた後、勝手に作業を始める | ユーザーが求めていない |
| 指示を受ける前に先回りして作業する | ユーザーの意図を無視している |
| 複数の解釈が可能な場合に勝手に判断する | 確認すべき |

## 曖昧な場合

入力が質問か指示か不明確な場合:

1. **確認する**: 「〜を実行しますか？それとも情報だけ必要ですか？」
2. **最小限の解釈**: 判断できない場合は質問として扱い、回答のみ

## 例

### 質問の例

```
User: このURLのページコンポーネントは何？

Claude: watch.$streamerId.before-entry.tsx です。
（ここで終了。勝手に中身を調べたり、問題を解決しようとしない）
```

### 指示の例

```
User: NotificationsModalの翻訳キーを「通知」に変更して

Claude:
1. ファイルを確認
2. 変更を実行
3. 結果を報告
```

### 曖昧な例

```
User: 認証周りにバグがありそう

Claude: 調査しますか？それとも現状の情報だけ必要ですか？
（勝手に調査を始めない）
```

## チェックリスト

回答する前に確認:

- [ ] これは質問か、指示か？
- [ ] 質問なら、答えだけで十分か？
- [ ] 指示なら、何を実行すべきか明確か？
- [ ] 余計なことをしようとしていないか？
